/**
 * Shared placeholders for the legal pages (privacy policy, terms of service).
 *
 * ⚠️ These are a STARTING TEMPLATE, not legal advice. Before public launch:
 *   1. Replace the placeholder company/contact values below with real ones.
 *   2. Have the privacy policy and terms reviewed by qualified counsel —
 *      especially the data-collection, third-party, and liability sections.
 */

export const LEGAL = {
  /** Legal entity that operates the site. */
  companyName: "[Company Name]",
  /** Public-facing product/site name. */
  siteName: "Offroad Parks",
  /** Contact address for privacy / legal inquiries. */
  contactEmail: "[legal@yourdomain.com]",
  /** Governing-law jurisdiction for the Terms. */
  jurisdiction: "[State/Country]",
  /** Date the current versions took effect. Update when you change the text. */
  effectiveDate: "July 15, 2026",
} as const;
